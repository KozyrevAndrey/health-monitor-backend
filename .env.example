# Domain for Traefik routing
DOMAIN=monitor.yourdomain.ru

# Basic Auth for the web dashboard
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=changeme

# App version tag (used during docker build)
VERSION=latest
